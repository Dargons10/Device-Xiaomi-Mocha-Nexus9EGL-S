diff --git a/camera/device/3.2/default/CameraDeviceSession.cpp b/camera/device/3.2/default/CameraDeviceSession.cpp
index 896c35843..294fe72dc 100644
--- a/camera/device/3.2/default/CameraDeviceSession.cpp
+++ b/camera/device/3.2/default/CameraDeviceSession.cpp
@@ -1377,9 +1377,17 @@ status_t CameraDeviceSession::constructCaptureResult(CaptureResult& result,
     // within the scope of this function
     result.frameNumber = frameNumber;
     result.fmqResultSize = 0;
-    result.partialResult = hal_result->partial_result;
-    convertToHidl(hal_result->result, &result.result);
+    // Legacy Tegra blobs may send invalid partial_result values (0 or >16).
+    // Sanitize to avoid framework issues.
+    if (hal_result->partial_result > 0 && hal_result->partial_result <= 16) {
+        result.partialResult = hal_result->partial_result;
+    } else {
+        ALOGI("%s: sanitizing partial_result %u -> 0 for frame %u",
+                __FUNCTION__, hal_result->partial_result, frameNumber);
+        result.partialResult = 0;
+    }
     if (nullptr != hal_result->result) {
+        convertToHidl(hal_result->result, &result.result);
         bool resultOverriden = false;
         Mutex::Autolock _l(mInflightLock);
 
@@ -1567,6 +1575,12 @@ void CameraDeviceSession::sProcessCaptureResult(
     CameraDeviceSession *d =
             const_cast<CameraDeviceSession*>(static_cast<const CameraDeviceSession*>(cb));
 
+    // Guard against null result from legacy Tegra blobs
+    if (hal_result == nullptr) {
+        ALOGE("%s: hal_result is null, ignoring", __FUNCTION__);
+        return;
+    }
+
     CaptureResult result = {};
     camera3_capture_result shadowResult;
     bool handlePhysCam = (d->mDeviceVersion >= CAMERA_DEVICE_API_VERSION_3_5);
diff --git a/graphics/composer/2.1/utils/hwc2on1adapter/HWC2On1Adapter.cpp b/graphics/composer/2.1/utils/hwc2on1adapter/HWC2On1Adapter.cpp
index 37021716c..78b6739d3 100644
--- a/graphics/composer/2.1/utils/hwc2on1adapter/HWC2On1Adapter.cpp
+++ b/graphics/composer/2.1/utils/hwc2on1adapter/HWC2On1Adapter.cpp
@@ -27,6 +27,7 @@
 
 #include <chrono>
 #include <cstdlib>
+#include <cutils/properties.h>
 #include <sstream>
 
 #include <hardware/hwcomposer.h>
@@ -2268,7 +2269,17 @@ void HWC2On1Adapter::populateCapabilities() {
     // Some devices might have HWC1 retire fences that accurately emulate
     // HWC2 present fences when they are deferred, but it's not very reliable.
     // To be safe, we indicate PresentFenceIsNotReliable for all HWC1 devices.
-    mCapabilities.insert(Capability::PresentFenceIsNotReliable);
+    //
+    // Mocha (Tegra K1): the NVIDIA GL composer syncs to vblank and its retire
+    // fences are reliable, so trusting them removes tearing. However, trusting
+    // them changes SurfaceFlinger's buffer release timing and this HWC1 blob
+    // then double-unregisters buffers (SIGSEGV in NvGrUnregisterBuffer). Keep
+    // it opt-in behind a property until the blob's buffer lifecycle is fixed.
+    char reliableFences[PROP_VALUE_MAX] = {0};
+    property_get("debug.mocha.reliable_fences", reliableFences, "0");
+    if (atoi(reliableFences) != 1) {
+        mCapabilities.insert(Capability::PresentFenceIsNotReliable);
+    }
 }
 
 HWC2On1Adapter::Display* HWC2On1Adapter::getDisplay(hwc2_display_t id) {
